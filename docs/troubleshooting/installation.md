# Problemas de instalación

Ejecuta `atlas-splitter doctor`. Si falta Geometry usa `atlas-splitter setup geometry`; si falta AI usa `atlas-splitter setup ai`. Reinicia la terminal tras instalar con pipx.
