# Segmentación visual

## Requisitos

Atlas WEBP local.

```text
atlas-splitter split atlas.webp --output resultados
```

La segmentación estima regiones por imagen. El reporte HTML y `review.json` ayudan a corregir ambigüedades. No infiere UV ni puede reconstruir un objeto 3D fiel.
