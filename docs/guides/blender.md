# Blender

Instala `atlas_splitter_blender.zip` desde **Edit > Preferences > Add-ons > Install from Disk**. En la barra lateral Atlas carga `project.json`, crea colecciones y usa **Reconstruir objetos**.

El add-on valida la versión del manifiesto y no duplica colecciones existentes.
