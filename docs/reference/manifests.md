# Manifiestos

`manifest.json` describe una separación visual. `objects_manifest.json` describe objetos UV y `project.json` agrupa atlas de una extracción. Los manifiestos versionados actuales usan `schema_version: "1.0"`.
