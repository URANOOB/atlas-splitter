# Estructura de salida

Cada atlas crea `png/`, `masks/`, `psd/`, `manifest.json`, `report/index.html` y, si se solicita, un ZIP. La agrupación añade `semantic_manifest.json`, grupos y previsualizaciones.
